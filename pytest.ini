[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    unit: pure logic, no I/O, no heavy models
    integration: requires disk or network access
    slow: takes more than a few seconds

addopts =
    -v
    --tb=short
    -m "not integration and not slow"
